<h1 align="center"> Welcome to <%= @user.username %>'s page </h1>

<div class="row">
    <div class="col-md-offset-5 center">
        <%= gravatar_for @user %>
    </div>
</div>

<h4 align="center"><%= @user.username %>'s articles </h4>
<%= render 'alpha_scaffolds/alpha_scaffold', obj: @user.alpha_scaffolds %>